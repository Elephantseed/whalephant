npm run stop:docker
docker rmi $(docker images --filter=reference='<%= userName %>/<%= appName %>-base:latest' --format "{{.ID}}") --force
npm run clean-volume:docker || echo "No volumes to remove"
docker system prune -f
