npm run stop:docker
docker rmi $(docker images --filter=reference='sloppylopez/whalephant-seed:latest' --format "{{.ID}}") --force
npm run clean-volume:docker &>/dev/null || echo "No volumes to remove"
docker system prune -f
