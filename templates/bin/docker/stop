docker-compose stop
docker stop $(docker ps -aqf "name=node-container") > /dev/null 2>&1 || echo "No node-container instances to remove"
docker stop $(docker ps -aqf "name=nginx-container") > /dev/null 2>&1 || echo "No nginx-container instances to remove"