docker-compose stop
docker stop $(docker ps -aqf "name=node-container") || echo "No node-container instances to remove"
docker stop $(docker ps -aqf "name=nginx-container") || echo "No nginx-container instances to remove"